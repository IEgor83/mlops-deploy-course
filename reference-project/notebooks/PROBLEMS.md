# Что не так с baseline_notebook.py

Эталонный разбор занятия 1. Каждый пункт закрывается на конкретном занятии —
это и есть программа курса.

| # | Проблема | Почему это ломает прод | Закрывается |
|---|---|---|---|
| 1 | Абсолютный путь `/Users/anna/Desktop/...` | Код не запустится ни у кого, кроме автора | зан. 1 |
| 2 | Имя файла `churn_final_v3_ПОСЛЕДНИЙ.csv` | Версии данных нет — метрика ни к чему не привязана | зан. 4 |
| 3 | `dropna()` без анализа | Молча выбрасывает клиентов первого месяца — целый сегмент | зан. 1 |
| 4 | `train_test_split` без `random_state` | Метрика скачет, улучшение не отличить от шума | зан. 3 |
| 5 | Ручной `map` категорий | Новая категория даст `NaN` и тихо испортит предсказание | зан. 1 |
| 6 | Нет `stratify` | При дисбалансе классов сплит нерепрезентативен | зан. 1 |
| 7 | `n_estimators=157` вшито в код | Откуда взялось — неизвестно, воспроизвести подбор нельзя | зан. 3 |
| 8 | Метрика печатается в stdout | Сравнить прогоны между собой невозможно | зан. 6 |
| 9 | Препроцессинг отдельно от модели | На инференсе его придётся повторить руками — training/serving skew | зан. 1, 10 |
| 10 | `pickle` в закомментированном коде, имя `model_v2_FINAL.pkl` | Какая модель в проде — не знает никто | зан. 7 |
| 11 | Нет фиксации версий библиотек | Тот же код на другом sklearn даёт другие числа | зан. 3 |
| 12 | Нет разделения val и test | Порог и гиперпараметры подбираются на тех же данных, где меряется качество | зан. 3 |
